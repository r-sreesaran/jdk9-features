group = 'org.wildcraft.jpms'
description = ''
ext.moduleName = 'org.wildcraft.jpms.implicit.ums.api'
dependencies {
    compile project(':jpms:versioning')
    compile project(':jpms:implicit-dependency:usergroup-api')
}
compileJava {
    inputs.property("moduleName", moduleName)
    doFirst {
        options.compilerArgs = [
                '--module-path', classpath.asPath,
        ]
        classpath = files()
    }
}